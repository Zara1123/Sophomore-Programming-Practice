把你踩过的坑整理成 Markdown（正好放进你的 GitHub 笔记仓库）：

|操作|Python|C/Java|
|:---:|:---:|:---:|
获取长度|len(s)|s.length()/strlen(s)|
for循环|for i in range(n)|for(int i=0;i<n;i++)|
条件|elif|else if|
代码块|缩进（4空格）|{}|
数组追加	ls.append(x)	需手动扩容 / ArrayList.add()


【数据结构】 = 逻辑关系 + 物理存储

Python 语言自带的类型 (高度封装，你直接用)
 ├── list (线性表 + 动态数组存储) 
 ├── str  (线性表 + 不可变数组存储)
 └── dict (集合 + 哈希表存储)

C 语言/Java 课让你手搓的 (底层原理，你要自己写)
 ├── 顺序表 (线性表 + 数组存储)  ≈  Python 的 list
 ├── 单链表 (线性表 + 链式存储)  ≈  Python 没有直接对应的，但底层常用
 └── 树/图  (非线性 + 链式存储)
概念	它到底是什么？
C/Java 原生数组	纯物理结构。连续存真实数据，死板但极速。
Python 的 list	动态指针数组。底层是 C 数组，存的全是指针，加了自动扩容魔法。
Python 的 tuple	静态指针数组。底层也是 C 数组，存指针，但没有扩容魔法，一旦创建大小固定，所以它不可变。
Python 的 str	静态字符数组。底层也是 C 数组，存字符，不可变。

看到“指针”、“离散分布”，就觉得“那这不就是链表吗？”

但答案是：它依然是数组（顺序表），绝对不是链表。真实数据离散了，但“装指针的数组”没离散。我们判断一种物理结构是数组还是链表，不是看里面存的是真实数据还是指针，而是看“存指针的那个容器本身，是不是连续的”。